# Configuración de Oracle Database MCP Server
# Copie este archivo a .env y configure según su entorno

# === CONFIGURACIÓN BÁSICA ===
# Host del servidor Oracle
ORACLE_HOST=host
# Puerto de Oracle (por defecto 1521)
ORACLE_PORT=port
# Nombre del servicio Oracle
ORACLE_SERVICE_NAME=service
# Usuario de base de datos
ORACLE_USERNAME=user
# Contraseña de base de datos
ORACLE_PASSWORD=pass

# === CONFIGURACIÓN DE POOL DE CONEXIONES ===
# Número mínimo de conexiones en el pool
ORACLE_POOL_MIN=2
# Número máximo de conexiones en el pool
ORACLE_POOL_MAX=10
# Incremento de conexiones
ORACLE_POOL_INCREMENT=1
# Timeout del pool en segundos
ORACLE_POOL_TIMEOUT=60

# === CONFIGURACIÓN DE RENDIMIENTO ===
# Tamaño del cache de statements
ORACLE_STMT_CACHE_SIZE=30
# Número de filas a traer por lote
ORACLE_FETCH_SIZE=100

# === CONFIGURACIÓN PARA VERSIONES ANTIGUAS DE ORACLE ===
# Activar para Oracle con criptografía antigua (pre-12c)
ORACLE_OLD_CRYPTO=true

# === CONFIGURACIÓN DEL CLIENTE ORACLE ===
# Directorio de las librerías del cliente Oracle (opcional)
# ORACLE_CLIENT_LIB_DIR=/usr/lib/oracle/19.3/client64/lib

# === CONFIGURACIÓN ADICIONAL ===
# Variables alternativas (compatibilidad)
ORACLE_USER=aygusercrm

# === CONFIGURACIÓN DE LOGGING ===
# Nivel de log (opcional)
# LOG_LEVEL=info 